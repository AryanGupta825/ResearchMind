services:
  - type: web
    name: researchmind
    runtime: python
    buildCommand: pip install -r requirements.txt && pip install langchain-groq
    startCommand: python app.py
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: TAVILY_API_KEY
        sync: false
